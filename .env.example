# Example environment variables for local development / docker-compose
# Copy this file to `.env` and edit the values before running the app.

# Application
PORT=3000
NODE_ENV=development

# Postgres database (used by docker-compose example)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=userdb

# Optional: other app-specific envs
#JWT_SECRET=your_jwt_secret
#OTHER_CONFIG=...
